<h1>修改評論</h1>


<%= turbo_frame_tag(@comment) do%>
  
  <%= form_with(model: @comment ) do |form| %>
    <%= form.text_area :content, class: 'mt-5 w-1/4 rounded', rows: 2 %>
    <%= form.submit class: 'btn btn-disable block mx-auto' %>
  <% end %>
  
<% end %>
  
